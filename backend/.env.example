# Access token signing secret (HMAC). REQUIRED for non-demo usage.
ACCESS_TOKEN_SIGNING_SECRET=change-me

# Token TTLs in seconds (optional)
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=604800
